var expect = require('expect');

describe('Application tests', () => {
  it('should properly run tests', () => {
    expect(1).toBe(1);
  });
  it('2nd test', () => {
    expect(1).toBe(1);
  });
});
